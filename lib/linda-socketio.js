// Generated by CoffeeScript 1.6.3
var Linda, fs, http, socketio, url;

http = require('http');

url = require('url');

fs = require('fs');

socketio = require('socket.io');

Linda = (function() {
  function Linda() {
    var _this = this;
    fs.readFile(__dirname + "/linda-socketio-client.js", function(err, data) {
      if (err) {
        throw "client js load error";
      }
      return _this.client_js_code = data;
    });
  }

  Linda.prototype.listen = function(opts) {
    var _this = this;
    if (opts == null) {
      opts = {
        io: null,
        server: null
      };
    }
    if (!(opts.io instanceof socketio.Manager)) {
      throw '"io" must be instance of Socket.IO';
    }
    if (!(opts.server instanceof http.Server)) {
      throw '"server" must be instance of http.Server';
    }
    this.io = opts.io;
    this.server = opts.server;
    this.server.on('request', function(req, res) {
      var _url;
      _url = url.parse(decodeURI(req.url), true);
      if (_url.pathname === "/js/linda-socketio.js") {
        res.writeHead(200);
        return res.end(_this.client_js_code);
      }
    });
    return this.io.sockets.on('connection', function(socket) {
      return socket.on('__linda_write', function(tuple) {
        return console.log("write tuple " + (JSON.stringify(tuple)));
      });
    });
  };

  return Linda;

})();

module.exports = new Linda;
